extends layout

block content
    h1 #{title}
    | we have #{submission_count} posts
    |   -  
    a(href='/submit') submit
    br
    br
    - for ( var i in submissions )
        - var sub = submissions[i]
        div
            a(href='/submissions/#{sub.id}') #{sub.title}
            |   -  #{sub.submitter}
        | #{sub.summary}
        br
        br


